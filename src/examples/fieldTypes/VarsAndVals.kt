package examples.fieldTypes

/**
 * Created by emirarditi on 02.07.2016.
 */

fun main(args: Array<String>) {
    /*there are two main definition techniques in Kotlin. They are:
    1)val = val's are like finals in java. You can't change them once they are assigned.
    2)var = like a regular java field.
     */
    var firstVar = 5;
    firstVar = firstVar + 1;
    val firstVal = 1;
    /*The statement below will not work so it is commented out.
    firstVal = firstV al+1;
    */
}
